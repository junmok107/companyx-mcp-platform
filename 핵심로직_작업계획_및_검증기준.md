# 핵심 로직 작업계획 및 검증 기준

> 담당: 나 (NL2SQL / 지식 그래프 / 벡터 검색 랭킹 — 3개 MCP 도구의 핵심 판단 로직)
> 기간: 8/22 ~ 8/24 (구현) + 8/25 ~ 8/26 (통합 테스트·튜닝)
> 공통 출력 포맷: 모든 도구는 `{ answer: string, raw_data: any, source: string }` 형식으로 반환 (라우터 연동 규격)

---

## 0. 개발 DB 환경 (8/22 확정)

Windows 네이티브 PostgreSQL(17/18)에는 pgvector가 없어서, **WSL(Ubuntu-22.04)에 PostgreSQL 14 + pgvector 0.7.4를 별도로 구축**했다.

| 항목 | 값 |
|---|---|
| 접속 | `host=localhost port=5434 user=postgres dbname=companyx` |
| 비밀번호 | 팀 채널에서 별도 공유 (코드/문서에 기록하지 않음) |
| 상태 | 스키마 9개 테이블 + 데이터 전체 적재 완료, `document_chunks`는 빈 테이블(임베딩 파이프라인 대기) |

**주의사항**
- WSL2는 유휴 상태가 되면 VM이 자동 종료되어 DB 접속이 끊긴다. 개발 세션 시작 전 WSL을 깨워두는 과정이 필요하다(백그라운드 keep-alive 프로세스 또는 `.wslconfig`의 `vmIdleTimeout` 설정).
- 각자 로컬에 동일하게 WSL+pgvector를 구축할지, 한쪽 PC의 DB를 공유해서 쓸지 팀원과 정해야 한다.
- 코드의 DB 접속 정보는 `COMPANYX_DB_DSN` 환경변수로 오버라이드 가능하며, 비밀번호는 `PGPASSWORD` 환경변수로만 전달한다 (레포가 대회 규정상 공개되므로 코드에 절대 하드코딩하지 않는다).

---

## 1. NL2SQL 도구

### 1-1. 작업 스텝

| Step | 내용 | 산출물 |
|---|---|---|
| 1 | 스키마 컨텍스트 준비 | `schema_context.md` — 테이블/컬럼 설명 + 예시 값 |
| 2 | 자연어→SQL 프롬프트 템플릿 | few-shot 포함 프롬프트 |
| 3 | 안전한 SQL 실행기 | 읽기 전용 실행 함수 |
| 4 | 결과 → 자연어 답변 변환 | 답변 생성 함수 |
| 5 | 테스트/튜닝 | 테스트 스크립트 + 결과 로그 |

### 1-2. Claude Code 프롬프트

**Step 1**
```
sql/01-schema.sql과 sql/erd.md를 읽고, NL2SQL 프롬프트에 넣을 스키마 설명 문서를
schema_context.md로 만들어줘. 각 테이블마다 컬럼명, 타입, 의미, 주요 값 예시(예: region은
서울/경기/부산 등)를 한 줄씩 정리해줘.
```

**Step 2**
```
questions.json에서 tool이 "nl2sql"인 항목 10개를 참고해서, 자연어 질문을 SQL로 변환하는
프롬프트 템플릿을 만들어줘. schema_context.md를 시스템 프롬프트에 포함하고, 질문 3~4개를
few-shot 예시로 넣어줘. 출력은 SQL 쿼리만 나오도록 제약 조건도 명시해줘.
```

**Step 3**
```
Python으로 PostgreSQL에 연결해서 SQL을 실행하는 함수를 만들어줘. LLM이 생성한 쿼리는
SELECT문만 허용하고, 세미콜론으로 여러 문장 실행되는 것과 DROP/DELETE/UPDATE 같은
쓰기 쿼리는 실행 전에 차단하도록 검증 로직을 넣어줘.
```

**Step 4**
```
SQL 실행 결과(행 목록)를 받아서 원래 질문에 대한 자연어 답변으로 요약하는 함수를 만들어줘.
LLM에게 질문 + 쿼리 결과를 같이 주고 한국어로 자연스럽게 답하게 해줘.
```

**Step 5**
```
questions.json의 nl2sql 카테고리 10개 질문을 순서대로 돌려서 결과를 출력하는 테스트 스크립트를
만들어줘. 틀린 케이스가 있으면 왜 SQL이 잘못 생성됐는지 원인을 같이 알려줘.
```

### 1-3. 검증 방법

1. `questions.json`의 `tool: "nl2sql"` 10개 질문을 테스트 스크립트로 일괄 실행
2. 각 질문에 대해 다음을 기록: 생성된 SQL / 실행 성공 여부 / 반환된 자연어 답변 / hint 필드 기준 정답 여부(수동 판정)
3. 별도로 쓰기 쿼리(INSERT/UPDATE/DELETE/DROP) 문자열을 포함한 질문을 3개 정도 임의로 만들어 실행기가 실제로 차단하는지 확인
4. 스키마에 없는 테이블/컬럼명을 생성하는지(hallucination) 로그에서 확인

### 1-4. 통과 기준

- [x] nl2sql 10문항 중 **8문항 이상** 정확한 SQL 생성 + 정확한 결과 반환 (80%) — **10/10 달성 (8/22 완료)**
- [x] 모든 실행 쿼리가 SELECT만 사용 — 쓰기 쿼리 실행 0건 (오프라인 검증 10케이스 + 실제 DB에서 다중문장 주입 시도 차단 확인)
- [x] 존재하지 않는 테이블/컬럼 참조 0건
- [x] 오답 2문항 이하는 원인 기록 — 아래 1-5 참고 (둘 다 원인 파악 후 프롬프트 수정으로 해결, 팀원 이관 불필요)

### 1-5. 디버깅 기록 (교훈)

1차 테스트에서 10문항 중 2건이 실패했다 (Q5 "기술지원팀 직원", Q7 "Critical 티켓").

- **원인 1 — few-shot 예시와 테스트 질문 문장 중복**: `prompt.py`의 few-shot 예시 하나가 `questions.json`의 실제 테스트 질문과 토씨 하나 안 틀리고 동일했다. 모델이 "이미 답변한 질문"으로 착각해 스키마 컨텍스트의 다른 예시 값(엉뚱한 부서명)을 끌어다 쓰는 현상이 발생 → **few-shot 질문은 반드시 실제 테스트셋과 겹치지 않게 패러프레이즈해야 한다.**
- **원인 2 — LLM의 ID 하드코딩 환각**: "기술지원팀" 질문에 대해 모델이 `dept_id = 6`을 임의로 추측해 넣었는데 실제로는 `기술지원팀 = 5`, `영업팀 = 6`이라 완전히 다른 부서 데이터를 반환했다. 규칙 문장("id를 추측하지 말 것")만으로는 부족했고, **동일 패턴의 few-shot 예시(JOIN으로 이름 필터링하는 예시)를 추가하고 나서야 해결**됐다.
- 추가로 `llm_client.py`에 `temperature=0`을 명시해 재현 가능한 디버깅이 가능하도록 함 (기본 temperature는 매 실행마다 다른 결과를 내서 원인 추적이 어려웠음).

---

## 2. 지식 그래프 도구

### 2-1. 작업 스텝

| Step | 내용 | 산출물 |
|---|---|---|
| 1 | 그래프 로딩 | `nodes.json` + `edges.json` → networkx DiGraph |
| 2 | 질문 → 엔티티/관계 추출 | 추출 프롬프트 |
| 3 | 그래프 순회 로직 | 1-hop/2-hop 탐색 함수 |
| 4 | 결과 → 자연어 변환 | 답변 생성 함수 |
| 5 | 테스트/튜닝 | 테스트 스크립트 + 결과 로그 |

### 2-2. Claude Code 프롬프트

**Step 1**
```
graph/nodes.json과 graph/edges.json을 읽어서 networkx DiGraph로 로드하는 함수를 만들어줘.
노드 타입(client/product/employee/project/department)과 관계 타입은 graph/schema.md 기준으로
맞춰줘.
```

**Step 2**
```
자연어 질문에서 대상 엔티티(예: "Client-A", "Product-C1")와 원하는 관계(USES, MANAGES_ACCOUNT 등)를
추출하는 프롬프트를 만들어줘. questions.json의 knowledge_graph 카테고리 10개를 예시로 참고해서
few-shot을 구성해줘.
```

**Step 3**
```
추출된 엔티티/관계를 받아서 networkx 그래프에서 1-hop, 2-hop 탐색하는 함수를 만들어줘.
graph/schema.md의 예시 질의(2홉 탐색 케이스 포함)를 참고해서 다중 홉도 처리되게 해줘.
```

**Step 4**
```
그래프 탐색 결과(노드/엣지 목록)를 받아서 원래 질문에 대한 자연어 답변으로 요약하는 함수를
만들어줘.
```

**Step 5**
```
questions.json의 knowledge_graph 카테고리 10개 질문을 순서대로 돌려서 결과를 출력하는
테스트 스크립트를 만들어줘. 틀린 케이스는 엔티티 추출 실패인지 순회 로직 실패인지 구분해서
알려줘.
```

### 2-3. 검증 방법

1. `questions.json`의 `tool: "knowledge_graph"` 10개 질문을 테스트 스크립트로 일괄 실행
2. 추출된 엔티티/관계가 `graph/nodes.json`, `graph/edges.json`에 실제 존재하는지 대조
3. 2-hop 이상 질문(예: "Product-D1 제품과 관련된 프로젝트는?")은 중간 노드까지 로그로 남겨 경로가 맞는지 확인
4. 존재하지 않는 노드/관계를 답변에 포함하는지(hallucination) 확인

### 2-4. 통과 기준

- [x] knowledge_graph 10문항 중 **8문항 이상** 정답 (80%) — **9/10 달성 (8/23 완료)**
- [x] 그래프에 없는 노드/관계 언급 0건
- [x] 2-hop 이상 질문 최소 2건 포함해 테스트, 전부 정확한 경로로 탐색 (Product-D1↔프로젝트 2홉 케이스 확인)
- [x] 엔티티 추출 실패로 인한 오답 원인 기록 — 아래 2-5 참고

### 2-5. 디버깅 기록 (교훈)

1차 테스트 10문항 중 4건이 실패했다 (Q2, Q5, Q8, Q9). 원인을 나눠보면:

- **원인 1 — 데이터셋 자체의 결함 (수정 불가)**: Q4 "서울물산 담당 엔지니어는 누구야?"에서 "서울물산"이라는 이름은 `graph/nodes.json`, `documents/`, `sql/` 어디에도 존재하지 않는다. `questions.json`의 hint는 `client_2`(=Client-B)를 가리키지만 질문 텍스트와 실제 데이터가 어긋나 있다 — **데이터셋 버그이며 우리 구현으로 고칠 수 없다.** 팀 차원에서 리원에이스 측에 문의하거나, 평가 시 이 1건은 예외로 처리해야 한다.
- **원인 2 — enum 값 환각**: Q8 "진행 중인 프로젝트"에서 모델이 `status: 'ongoing'`을 지어냈다 (실제 값은 `in_progress`). NL2SQL의 ID 하드코딩 문제와 같은 패턴 → **그래프 노드 속성의 실제 값 목록(`PROPERTY_VALUE_NOTES`)을 추출 프롬프트에 명시하고 나서 해결**됨.
- **원인 3 — 답변 생성 단계에서 유효한 결과를 "없음"으로 오판**: Q2, Q5, Q9는 그래프 순회 자체는 정확했는데(직접 확인함), 답변 생성 LLM 호출이 결과 노드 목록을 보고도 "관련 정보가 없다"고 잘못 답했다. 질문에 언급된 키워드(예: "Product-C1")가 결과 노드의 속성 값에 그대로 나타나지 않아서 모델이 연결을 못 지은 것으로 추정. 프롬프트를 더 명시적으로 바꿔도 완전히 해결되지 않아, **결과 노드가 존재하는데 LLM이 부정 표현("없습니다" 등)을 쓰면 결정론적 템플릿 답변으로 강제 대체하는 방어 로직**을 추가해 해결함. 소형 로컬 모델(gemma2:9b)의 신뢰성 한계를 코드 레벨에서 보완한 사례.

---

## 3. 벡터 검색 랭킹 로직

> 전제: 팀원의 임베딩 파이프라인(`document_chunks` 적재)이 먼저 완료되어야 시작 가능

### 3-1. 작업 스텝

| Step | 내용 | 산출물 |
|---|---|---|
| 1 | 질의 임베딩 + 유사도 검색 | Top-K 검색 함수 |
| 2 | 리랭킹 | 문서 타입/키워드 가중치 로직 |
| 3 | 컨텍스트 추출 + 답변 생성 | 답변 생성 함수 |
| 4 | 테스트/튜닝 | 테스트 스크립트 + 결과 로그 |

### 3-2. Claude Code 프롬프트

**Step 1**
```
document_chunks 테이블(팀원이 적재한 embedding 컬럼)에 대해, 질의를 같은 임베딩 모델로
벡터화하고 pgvector의 코사인 유사도(<=>)로 Top-K를 가져오는 함수를 만들어줘.
```

**Step 2**
```
Top-K 후보에 대해 문서 타입(documents/index.json의 type: incident_report/technical_doc/
meeting_note/proposal)과 질문 키워드 매칭을 가중치로 반영하는 간단한 리랭킹 로직을 추가해줘.
```

**Step 3**
```
리랭킹된 chunk들을 컨텍스트로 LLM에 전달해서 질문에 대한 답변을 생성하는 함수를 만들어줘.
답변에는 참고한 문서 ID(DOC-XXX)를 source로 같이 반환해줘.
```

**Step 4**
```
questions.json의 vector_search 카테고리 10개 질문을 순서대로 돌려서 결과를 출력하는
테스트 스크립트를 만들어줘. 각 질문마다 Top-3 검색 결과의 문서 ID도 같이 출력해줘.
```

### 3-3. 검증 방법

1. `questions.json`의 `tool: "vector_search"` 10개 질문을 테스트 스크립트로 일괄 실행
2. 질문과 관련된 문서(hint 및 `documents/index.json`의 title로 유추한 정답 문서)가 Top-3 검색 결과에 포함되는지 확인
3. 리랭킹 적용 전/후 순위 비교 로그를 남겨 리랭킹이 실제로 개선되는지 확인
4. 답변에 근거 문서(source)가 실제로 명시되는지 확인

### 3-4. 통과 기준

- [x] vector_search 10문항 중 **8문항 이상** Top-3 안에 관련 문서 포함 (Recall@3 ≥ 80%) — **10/10 달성 (8/23 완료)**
- [x] 모든 답변에 근거 문서 ID(source)가 최소 1개 이상 포함
- [x] 완전히 무관한 문서 타입이 상위에 노출되는 사례 0건
- [x] 리랭킹 적용 후 Recall@3가 적용 전보다 하락하지 않음

### 3-5. 디버깅 기록 (교훈)

원래 임베딩 파이프라인은 팀원 담당이지만, 벡터 검색 랭킹 로직을 막고 있어서 최소 구현으로 대신 만들었다(`vector_search/embed.py`) — 팀원 검토/교체 필요. Ollama `nomic-embed-text`(768차원, 스키마와 일치)로 문서 40건·162~186청크를 적재했다.

1차 테스트 10문항 중 6건이 실패했는데, 원인은 3가지로 나뉜다.

- **원인 1 — 임베딩 프리픽스 누락**: `nomic-embed-text`는 문서는 `search_document:`, 질의는 `search_query:` 프리픽스를 붙여야 정상 동작하는 모델인데 빠뜨렸다. 그 결과 질문 내용과 무관하게 같은 문서 3개(DOC-002/003/009)가 계속 상위에 나오는 현상이 발생 → **프리픽스 추가 후 해결**.
- **원인 2 — 청킹이 너무 굵음**: 마크다운 `## ` 레벨로만 쪼개서, `### 백업`처럼 더 세분화된 하위 섹션이 모니터링·로그 관리 등과 한 청크에 뭉쳐 임베딩이 희석됐다 (예: "백업 정책" 질문에 실제 백업 설명 청크가 186개 중 108위로 밀림) → **존재하는 가장 세부적인 헤더 레벨(`###`)로 쪼개도록 수정**.
- **원인 3 — 답변 생성 단계에서 유효한 결과를 "없음"으로 오판**: 지식 그래프 도구에서 겪은 것과 동일한 패턴. 컨텍스트에 정답 청크가 있는데도 LLM이 "관련 문서가 없다"고 잘못 답하는 경우가 반복됨 → **같은 방어 로직(결과가 있는데 부정 표현이 나오면 템플릿 답변으로 대체)을 적용해 해결**. 이 소형 모델의 반복되는 실패 패턴이므로, 세 도구 공통으로 이 가드를 기본 적용하는 게 좋겠다.
- 추가로 코퍼스가 작을 때(186청크 수준)는 1차 벡터 검색 후보군을 좁게(8~20개) 가져오면 정답 청크가 애초에 후보에서 빠지는 문제가 있어, **1차 후보군을 100개로 넓히고 재순위 로직이 그 안에서 정렬하도록 조정**했다.

---

## 4. 통합 검증 (8/25~26)

3개 도구 각각의 통과 기준을 만족한 뒤, 아래 기준으로 전체를 재검증한다.

| 검증 항목 | 방법 | 기준 |
|---|---|---|
| 개별 도구 정확도 | 각 도구 10문항씩 재실행 | 도구별 8/10 이상 유지 |
| 회귀 테스트 | 이전에 통과했던 케이스가 이후 수정으로 깨지지 않았는지 30문항 전체 재실행 | 기존 통과 케이스 유지율 100% |
| 응답 형식 일관성 | 3개 도구 모두 `{answer, raw_data, source}` 형식 반환 확인 | 형식 불일치 0건 |
| Hallucination 점검 | DB/그래프/문서에 없는 내용을 답변에 포함하는지 전수 확인 | 0건 |

각 도구 수정 시 **해당 도구 10문항 + 전체 30문항 회귀 테스트**를 같이 돌려서, 한 도구 수정이 다른 도구나 라우팅에 영향을 주지 않는지 확인한다.
