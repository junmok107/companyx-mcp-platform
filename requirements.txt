# Company-X MCP 플랫폼 — Python 런타임 의존성
# 검증 환경(독립 감사 커밋 기준)에서 실제로 설치된 버전으로 고정한다.
# Ollama LLM 호출은 표준 라이브러리(urllib)만 쓰므로 별도 패키지가 없다.
#
# 설치:  pip install -r requirements.txt
# Python 3.11+ 필요.

# ── 핵심: MCP 서버 + 3개 도구 ──
mcp==2.0.0          # server.mcpserver.MCPServer 경로는 mcp 2.x 전용(1.x의 fastmcp와 다름). 반드시 고정.
psycopg==3.3.4      # PostgreSQL 드라이버 — NL2SQL 조회 실행
networkx==3.6.1     # 지식 그래프 순회

# ── 웹 프론트엔드 연동용 HTTP 브릿지(bridge/server.py) ──
fastapi==0.138.0
uvicorn==0.49.0
pydantic==2.13.4    # fastapi/mcp가 요구(명시 고정)

# ── (선택) router_ml/ — 증류 라우터 실험. 실패로 보존한 코드이며 본 서비스 실행에는 불필요. ──
# 재현하려면 아래 주석을 해제해 설치한다.
# numpy==2.3.0
# scipy==1.18.0
# scikit-learn==1.9.0
# joblib==1.5.3
